# 🚀 Инструкция по запуску TrustEx Bot

## ✅ Что было исправлено:

### Критические ошибки:
1. ✅ **Создан файл start.js** - теперь можно запускать через `npm start`
2. ✅ **Добавлен WEB_APP_URL в .env** - бот теперь корректно работает
3. ✅ **Исправлена опечатка в bot.js** - "USDBT" → "USDT"
4. ✅ **Исправлена опечатка в binance.js** - "RUBHSD" → "RUBUSD"

### Проверено и работает:
- ✅ Все контроллеры (profile, portfolio, trade, transaction, verification, analytics)
- ✅ База данных (dbManager.js) 
- ✅ API Routes (api.js)
- ✅ Middleware (errorHandler.js)
- ✅ Utils (binance.js, paths.js)
- ✅ Public files (HTML/CSS/JS)
- ✅ Package.json с правильными скриптами

---

## 📦 Установка зависимостей

```bash
cd nexo-trade
npm install
```

## ⚙️ Настройка .env файла

Файл `.env` уже создан! Проверь и при необходимости измени:

```env
# Твой токен уже установлен:
TELEGRAM_BOT_TOKEN=7970832761:AAHZtUVW59Gn_Qn1NJn5L9a0sXFhUin1IQM
BOT_USERNAME=trustEx_ru_bot

# Для локальной разработки:
WEB_APP_URL=http://localhost:3000
PORT=3000
NODE_ENV=development

# Для продакшена (Railway/Render):
# WEB_APP_URL=https://your-app.up.railway.app
# NODE_ENV=production
```

---

## 🎯 Запуск бота

### Вариант 1: Все вместе (рекомендуется)
```bash
npm start
```
Запустит и API сервер и Telegram бота одновременно.

### Вариант 2: Раздельно (для отладки)

**Терминал 1 - API сервер:**
```bash
npm run api
```

**Терминал 2 - Telegram бот:**
```bash
npm run bot
```

---

## 🔍 Проверка работы

### 1. API сервер
Открой в браузере: http://localhost:3000

Должна загрузиться главная страница.

### 2. Проверка API endpoints
```bash
# Проверить здоровье API
curl http://localhost:3000/api/

# Создать тестового пользователя
curl -X POST http://localhost:3000/api/user -H "Content-Type: application/json" -d "{\"telegramId\": 123456}"
```

### 3. Telegram бот
1. Открой Telegram
2. Найди бота: **@trustEx_ru_bot**
3. Отправь команду `/start`
4. Бот должен ответить с меню

---

## 📱 Структура команд бота

| Команда | Описание |
|---------|----------|
| `/start` | Начало работы, регистрация |
| `/profile` | Твой профиль и балансы |
| `/stats` | Статистика торговли |
| `/deposit` | Пополнить счет |
| `/withdraw` | Вывести деньги |
| `/trade` | Создать сделку |
| `/analytics` | Аналитика за период |
| `/help` | Справка |

---

## 🐛 Решение проблем

### Проблема: "TELEGRAM_BOT_TOKEN не установлен"
**Решение:** Проверь файл `.env`, токен должен быть там.

### Проблема: "Cannot find module"
**Решение:** 
```bash
npm install
```

### Проблема: "Port 3000 already in use"
**Решение:** 
```bash
# Windows
netstat -ano | findstr :3000
taskkill /PID <номер_процесса> /F

# Или измени PORT в .env на другой (например, 3001)
```

### Проблема: "База данных не найдена"
**Решение:** База создастся автоматически при первом запуске. Если нет - проверь, что файл `database.json` существует в корне проекта.

---

## 🚀 Деплой на Railway.app

1. Зайди на https://railway.app и создай проект
2. Подключи GitHub репозиторий или загрузи файлы
3. Добавь Environment Variables:
   ```
   TELEGRAM_BOT_TOKEN=7970832761:AAHZtUVW59Gn_Qn1NJn5L9a0sXFhUin1IQM
   BOT_USERNAME=trustEx_ru_bot
   NODE_ENV=production
   PORT=3000
   ```
4. После деплоя получи публичный URL (например: `your-app.up.railway.app`)
5. Добавь переменную:
   ```
   WEB_APP_URL=https://your-app.up.railway.app
   ```
6. Railway автоматически запустит `npm start`

---

## 📚 Архитектура проекта

```
nexo-trade/
├── start.js           ✅ Запускает API + бота
├── index.js           ✅ API сервер (Express)
├── bot.js             ✅ Telegram бот
├── app.js             ✅ Express конфигурация
├── .env               ✅ Переменные окружения
├── database.json      ✅ База данных (JSON)
│
├── controllers/       ✅ Логика бизнеса
│   ├── analyticsController.js
│   ├── portfolioController.js
│   ├── profileController.js
│   ├── tradeController.js
│   ├── transactionController.js
│   └── verificationController.js
│
├── models/            ✅ Работа с данными
│   └── dbManager.js
│
├── routes/            ✅ API маршруты
│   └── api.js
│
├── middleware/        ✅ Обработка ошибок
│   └── errorHandler.js
│
├── utils/             ✅ Утилиты
│   ├── binance.js
│   └── paths.js
│
└── public/            ✅ Фронтенд (HTML/CSS/JS)
    ├── index.html     - Главная страница
    ├── trading.html   - Торговля
    ├── terminal.html  - Терминал
    ├── deposit.html   - Депозит
    ├── withdraw.html  - Вывод
    └── app.js         - JavaScript логика
```

---

## ✨ Фичи проекта

- 💰 **Пополнение/Вывод** - Управление балансом
- 📈 **Торговля** - Обмен криптовалют с Binance курсами  
- 📊 **Аналитика** - Статистика и графики P&L
- 👤 **Профиль** - Кошельки USDT, RUB, BTC, ETH, TON
- ✅ **Верификация** - Система подтверждения пользователей
- 🤖 **Telegram бот** - Полное управление через Telegram
- 🌐 **Web App** - Красивый интерфейс через Telegram Mini App

---

## 🎉 Готово!

Теперь твой бот полностью рабочий. Все ошибки исправлены, все файлы на месте.

**Запускай:**
```bash
npm start
```

**Тестируй в Telegram:**
@trustEx_ru_bot → `/start`

---

**Удачи с проектом! 🚀**
