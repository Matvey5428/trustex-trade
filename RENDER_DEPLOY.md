# 🚀 Деплой TrustEx на Render.com

## 📋 Требования

- Аккаунт на [Render.com](https://render.com)
- Аккаунт на [GitHub](https://github.com)
- Токен Telegram бота от [@BotFather](https://t.me/BotFather)

---

## 🎯 Шаг 1: Загрузка на GitHub

### Если у тебя еще нет репозитория:

```bash
cd nexo-trade

# Инициализация Git
git init

# Добавляем все файлы
git add .

# Первый коммит
git commit -m "Initial commit - TrustEx Trading Bot"

# Создай репозиторий на GitHub, затем:
git remote add origin https://github.com/ТВОЙ_USERNAME/trustex-bot.git
git branch -M main
git push -u origin main
```

### Если репозиторий уже есть:

```bash
git add .
git commit -m "Ready for Render deployment"
git push
```

---

## 🎯 Шаг 2: Создание сервиса на Render

### Вариант A: Через Blueprint (автоматически)

1. Зайди на https://render.com
2. Нажми **"New"** → **"Blueprint"**
3. Подключи свой GitHub репозиторий
4. Render автоматически найдет `render.yaml` и создаст сервис
5. Перейди к **Шагу 3** для настройки переменных

### Вариант B: Вручную (рекомендуется)

1. Зайди на https://dashboard.render.com
2. Нажми **"New +"** → **"Web Service"**
3. Выбери **"Build and deploy from a Git repository"**
4. Подключи свой GitHub аккаунт
5. Выбери репозиторий с TrustEx

---

## 🎯 Шаг 3: Настройка Web Service

### Основные настройки:

| Поле | Значение |
|------|----------|
| **Name** | `trustex-bot` |
| **Region** | `Frankfurt (EU Central)` ← ближе к России |
| **Branch** | `main` |
| **Runtime** | `Node` |
| **Build Command** | `npm install` |
| **Start Command** | `npm start` |
| **Instance Type** | `Free` |

### Нажми **"Create Web Service"**

---

## 🎯 Шаг 4: Настройка Environment Variables

После создания сервиса:

1. Перейди в **Dashboard** → Твой сервис → **"Environment"**
2. Нажми **"Add Environment Variable"**
3. Добавь эти переменные:

| Key | Value | Описание |
|-----|-------|----------|
| `NODE_ENV` | `production` | Режим продакшена |
| `PORT` | `10000` | Порт Render (по умолчанию) |
| `TELEGRAM_BOT_TOKEN` | `7970832761:AAHZtUVW59Gn_Qn1NJn5L9a0sXFhUin1IQM` | Твой токен бота |
| `BOT_USERNAME` | `trustEx_ru_bot` | Username бота |
| `WEB_APP_URL` | `https://trustex-bot.onrender.com` | URL твоего сервиса* |
| `API_BASE_URL` | `https://trustex-bot.onrender.com/api` | URL API* |

> ⚠️ *URL появится после первого деплоя. Сначала добавь остальные переменные, задеплой, получи URL, затем обнови WEB_APP_URL и API_BASE_URL.

4. Нажми **"Save Changes"**

---

## 🎯 Шаг 5: Первый деплой

1. После сохранения переменных, сервис автоматически начнет деплой
2. Подожди 2-5 минут пока Render установит зависимости и запустит приложение
3. Следи за логами в разделе **"Logs"**

### Успешный деплой выглядит так:

```
==> Building...
==> Installing dependencies...
npm WARN deprecated...
added 50 packages in 5s

==> Build successful 🎉
==> Starting service...
🚀 TrustEx Trading Platform
================================
📡 Запуск API сервера...
🚀 Сервер запущен: http://localhost:10000
🤖 Запуск Telegram бота...
✅ Web App URL: https://trustex-bot.onrender.com
🤖 Telegram бот запущен (режим Polling)
```

---

## 🎯 Шаг 6: Получение URL и финальная настройка

1. Перейди в **Dashboard** → Твой сервис
2. В верхней части страницы увидишь URL вида:
   ```
   https://trustex-bot.onrender.com
   ```
3. Скопируй этот URL
4. Перейди в **"Environment"** и обнови:
   - `WEB_APP_URL` = `https://trustex-bot.onrender.com`
   - `API_BASE_URL` = `https://trustex-bot.onrender.com/api`
5. Нажми **"Save Changes"** - сервис автоматически перезапустится

---

## ✅ Шаг 7: Проверка работы

### Проверь API:
Открой в браузере:
```
https://trustex-bot.onrender.com/api/
```

Должен вернуться JSON:
```json
{
  "status": "ok",
  "service": "TrustEx API",
  "version": "1.0.0",
  "timestamp": "2026-02-26T..."
}
```

### Проверь Web App:
```
https://trustex-bot.onrender.com
```

Должна открыться главная страница.

### Проверь Telegram бота:
1. Открой Telegram
2. Найди @trustEx_ru_bot
3. Отправь `/start`
4. Нажми кнопку **"🚀 Открыть биржу"**
5. Должно открыться Mini App!

---

## 🐛 Решение проблем

### Проблема: Бот не отвечает
**Причины:**
- Сервис еще запускается (подожди 1-2 минуты)
- Неверный TELEGRAM_BOT_TOKEN
- Сервис "заснул" (на бесплатном плане)

**Решение:**
1. Проверь логи в Render Dashboard → Logs
2. Убедись что токен правильный
3. Открой URL в браузере чтобы "разбудить" сервис

### Проблема: Web App не открывается
**Причина:** Неверный WEB_APP_URL

**Решение:**
1. Проверь что WEB_APP_URL начинается с `https://`
2. URL должен быть без `/` в конце
3. Пример: `https://trustex-bot.onrender.com`

### Проблема: "Service Unavailable"
**Причина:** Сервис еще деплоится или упал

**Решение:**
1. Проверь статус в Dashboard
2. Посмотри логи на ошибки
3. Если ошибка в коде - исправь и сделай git push

### Проблема: Сервис "засыпает"
**Причина:** Бесплатный план Render "усыпляет" сервис после 15 минут неактивности

**Решения:**
1. **Простое:** Первый запрос после сна занимает 30-60 секунд
2. **Платное:** Перейти на платный план ($7/месяц)
3. **Хак:** Использовать cron-job.org для пинга каждые 14 минут:
   ```
   URL: https://trustex-bot.onrender.com/api/
   Интервал: каждые 14 минут
   ```

---

## 📊 Мониторинг

### Логи в реальном времени:
Dashboard → Твой сервис → **Logs**

### Метрики:
Dashboard → Твой сервис → **Metrics**
- CPU usage
- Memory usage
- Request count

### Алерты:
Dashboard → Твой сервис → **Settings** → **Health & Alerts**

---

## 🔄 Обновление приложения

После изменений в коде:

```bash
git add .
git commit -m "Update: описание изменений"
git push
```

Render автоматически обнаружит push и начнет новый деплой!

---

## 💰 Планы Render

| План | Цена | RAM | CPU | Sleep |
|------|------|-----|-----|-------|
| **Free** | $0 | 512 MB | Shared | После 15 мин |
| **Starter** | $7/мес | 512 MB | 0.5 | Нет |
| **Standard** | $25/мес | 2 GB | 1 | Нет |

Для Telegram бота достаточно **Free** плана, но с "засыпанием".
Для стабильной работы рекомендую **Starter** ($7/мес).

---

## 🎉 Готово!

Твой бот задеплоен и работает на Render.com! 🚀

**URL сервиса:** `https://trustex-bot.onrender.com`
**Telegram бот:** `@trustEx_ru_bot`

---

## 📞 Быстрая справка

| Действие | Команда/Ссылка |
|----------|----------------|
| Dashboard | https://dashboard.render.com |
| Логи | Dashboard → Logs |
| Перезапуск | Dashboard → Manual Deploy |
| Переменные | Dashboard → Environment |
| Домен | Dashboard → Settings → Custom Domains |

---

Made with ❤️ for TrustEx
